<form method="post" action="{{ path('user_delete', {'id': user.id}) }}" onsubmit="return confirm('Cet utilisateur sera supprimé, cette action est irréversible, êtes vous sûr ?' );">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button type="submit" class="btn btn-small waves-effect waves-light"> Refuser </button>
</form>
