{% extends 'base.html.twig' %}
{% block title %} Mon compte {% endblock %}
{% block stylesheets %}
    <link href="{{ asset('assets/css/update-user.css') }}" rel="stylesheet" />
{% endblock %}

{% block body %}
    {% if (is_granted("ROLE_EMPLOYEE")) %}
        {% include('headers/header-employee.html.twig') %}
    {% else %}
        {% include('headers/header-user.html.twig') %}
    {% endif %}

    <main>
        {% include('./_app-flashes.html.twig') %}
        <div class="row center">
            <h1>Mon profil</h1>
        </div>
        <div class="row center introduce-container">
            <p> Gérer votre profil et changer certaines informations personnelles de votre compte.</p>
        </div>
        <div class="container">
            <div class="error-container center">
                {{ form_errors(form) }}
            </div>
            {% include('users/_form.html.twig') %}
        </div>
    </main>

    {% include('./_footer.html.twig') %}

    <script src="{{ asset('assets/js/init-modal.js') }}" defer></script>
    <script src="{{ asset('assets/js/init-dropdown.js') }}" defer></script>
    <script src="{{ asset('assets/js/passViewerIcon.js') }}" defer></script>
    <script src="{{ asset('assets/js/btnDisabledUpdateUser.js') }}" defer></script>

{% endblock %}